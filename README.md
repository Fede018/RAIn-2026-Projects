# RAIn-2026-Projects
## 📦 Librerías necesarias

Para ejecutar correctamente los puntos del **TP1**, es necesario contar con las siguientes librerías de Python:

```python
import flet as ft
import fitz
import re
import collections
```

Para ejecutar correctamente los puntos del **TP2**, es necesario contar con las siguientes
librerías de Python

```python
import os
import nltk
from nltk.util import ngrams
```
El script incluirá automáticamente la descarga de los recursos `punkt` y `punkt_tab` de NLTK la primera vez que se ejecute.
